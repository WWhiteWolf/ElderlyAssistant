# Parked items — running backlog

Future / deferred work for "Remember When." Not for the current session — the active goal lives in `handoff.md`. Pull an item from here when you're ready to take it on; move it back into `handoff.md` once it's the live goal. Add new ideas as they come up.

Last updated: 2026-06-27 (session #25 — the two #25 bugs handled: **Timer cancel — ✅ FIXED + device-validated + committed** (was cancelling with a made-up clock id, not the real notification id); **To-Do "Done" date — ✅ FIXED in code, uncommitted, untested** (now stamps the reminder's FIRE time, like My Day). The **OK** button was confirmed working in real use (clears just that one reminder, no code change). Then the timer work grew into a **NEW gentle/urgent NAG FEATURE** (built, `tsc` clean, UNCOMMITTED) with a **KNOWN UNRESOLVED BUG: tapping the banner / Dismiss doesn't stop the nags** — next session (#26) starts there; full detail + diagnosis plan in handoff "#25". Loud-alert custom sound (needs a rebuild) and Apple Critical Alerts are deferred follow-ons. — Earlier, session #24 — **Local backup feature COMPLETE + DEVICE-VERIFIED** — Import half BUILT and validated across all cases (correct password restores Vault, wrong password refuses, non-backup file rejected). With Export (#23) the whole feature works end to end; all in `app/backup.tsx`. **Note: Export+Import were both built in the single real session named "restart #23"; Patrick device-verified the whole feature in that #23 build, including the real iCloud round-trip — exported to iCloud and imported back on the phone. So the "real-device iCloud pass" is DONE, not pending.** New native module first-used: `expo-document-picker`. Next candidate: the parked #22 security cleanup. See handoff "#24". — Earlier, session #23 — **Backup Export half BUILT + Simulator-validated** on both the empty-Vault and encrypted-Vault paths. One app file (`app/backup.tsx`); added a now-**required** native dep `react-native-get-random-values` (crypto-js needs it for the AES salt RNG — must be in every future rebuild/EAS build). NEXT = build the Import half (shell already exists). See handoff "#23". — Earlier, session #22 — **Security: the two real Vault exposures CLOSED + Simulator-tested** — the toggle bypass (`a8511c3`) and the Vault Face ID re-prompt loop (`3bd445e`). Remaining PIN cleanup (Reset All Data off the PIN, remove dead Change PIN row, decide on orphaned screens) is **non-urgent, not an exposure**; Patrick forgot the PIN and no data is at risk, so the PIN can be fully retired. See handoff "#22". — Earlier, session #20 — Siri voice **PARKED by Patrick**. Did the research (now in `siri-cursor-brief.md` UPDATE 3) + tried the cheapest fix (entity `DisplayRepresentation` → `LocalizedStringResource(stringLiteral:)`, committed `2118681`, built, device-tested) — it did **NOT** fix voice. Confirmed the Expo/CNG build pipeline is fine (`Metadata.appintents` generates; intents in the Sources phase), so a native rewrite wouldn't differ. Patrick's hard requirement — *live, no-rebuild recognition* — rules out the fixed `AppEnum` option. No high-confidence fix remains; `IndexedEntity` is a genuine maybe, and a documented iOS 26.4 App Intents regression (Patrick on 26.5) means part of this may be Apple-side. **Decision: park voice — "either a way appears later, or go without it."** Tap-from-Shortcuts already works; all Siri scaffolding stays in place, inert. If revisited, cheapest decisive test = run Apple's "BooksShelf" dynamic-entity sample on the device. See handoff "THIS SESSION — #20" + `docs/siri-cursor-brief.md`.)

---

## In plain English — what's on this list (read this first)

This is the "someday" list: things worth doing eventually, but not what we're working on right now. The technical entries below each have a short everyday-language version here so you can tell at a glance what each one is.

**Things that are a bit broken (Bugs / correctness):**

- **Timer cancel might not work — ✅ FIXED + DEVICE-VALIDATED + COMMITTED (session #25).** It really was using the wrong tag: the app cancelled with a made-up clock number instead of the real id the phone gave back, so the main "Timer Done" alert still fired. Fixed, and Patrick confirmed on the Simulator AND phone — a cancelled timer no longer fires. (The timer code has since been rewritten for the new nag feature below; that rewrite is uncommitted.)
- **Medication taken after midnight recorded on the wrong day — My Day — FIXED + DEVICE-VALIDATED 2026-06-25 (session #15).** Patrick's medication is a **My Day** daily routine item, taken before bed, sometimes after midnight, marked **Done from the notification popup**. The bug: banner Done wrote **no** history line (only flipped the item complete), and the next-day reset cleared that flag → no durable record; and any record that was written used the tap-moment date → wrong day after midnight. **Fix (`app/_layout.tsx`):** banner Done now writes a dated history entry, with date + time taken from when the reminder **fired** (`response.notification.date`×1000 — iOS reports it in seconds), so a dose marked just after midnight files under the day the bedtime reminder was issued. **✅ Phone test (2026-06-25):** Medication fired 11:55 PM on 06/24, Done tapped after midnight → Log shows **06/24 | 23:55 | Medication** (prior day, fire time); Patrick happy with the behavior. **Pets Day mirrors this** (writes to `pets_history`) per Patrick's request — **same code path, not separately device-tested.** **Left as-is:** the on-screen **Log** button still uses tap-time (no fired-notification date to borrow; a cutoff-hour rule was discussed and deferred). Known edge: a Snooze crossing midnight files under the new day. (The To-Do "Done" date bug below is separate and still unfixed.)
- **"Repeating" To-Dos — Weekly/Monthly/Yearly now repeat; only 3 & 6 Months remain.** Weekly (session 6), then **Monthly + Yearly** (session #8, 2026-06-19) now fire true repeating alerts. **Only "3 Months" and "6 Months" still do nothing** — iOS has no native every-3/6-month alarm, and those two options have no date picker in the form to anchor them. (Monthly/Yearly committed, device test pending; see handoff.)

- **To-Do banner Done + Snooze — DONE (banner). On-tile Snooze button still open.** Session 6 (2026-06-19) gave To-Do reminders a **Done + Snooze 15/30/60 banner** (new `todosnooze` category). NOT yet added: an **on-page Snooze button on each To-Do tile** like My Day/Pets have — do that if Patrick wants it. (Awaiting device test.)
- **Leftover old pet data.** When Pets Day was rebuilt, it switched to new storage and left the old pet data behind, sitting unused in the app. It's harmless, but a small cleanup would tidy it away.

**Decisions to make (Design decisions):**

- **Where should daily-repeating reminders live?** My Day already handles daily things well. The question is whether repeating reminders should live only in My Day, leaving To-Do just for one-time, dated reminders. Not decided yet.
- **Pets Day reminders — DONE.** Pets Day now sends daily routine reminders with Snooze, the same way My Day does. Built and tested on the phone 2026-06-17.
- **Tapping a reminder only opens the screen, not the item.** When you tap a reminder notification it takes you to the right screen, but not to the exact item — and that behavior has never actually been tested with a real tap.
- **Per-appointment reminder time override.** The morning/evening reminder times are global (set once in Settings). Patrick chose to keep it global only. A possible later add-on: let a single appointment use a different morning/evening time than the global default. Parked, not planned.

**Nice-to-have later (UI polish):**

- **Sort the To-Do list by soonest first — DONE + DEVICE-VALIDATED 2026-06-23.** Built session #11 (one fixed sort by due date+time, sort buttons removed), validated on the phone session #12: the list shows the soonest item on top, with its time. Cleared.
- **Project Planner reminders do nothing yet.** That screen has reminder fields, but they aren't wired up to anything. Low priority.

**New feature being considered (likely next goal):**

- **Audio input — talking to the app instead of typing. DIRECTION SET 2026-06-24 (session #14).** Goal is **hands-free** control. Decided: the custom in-app mic is dropped (iPhone's keyboard dictation mic already types by voice); a custom "wake word" inside our app is impossible on iPhone (Apple lets only Siri listen in the background). So hands-free goes **through Siri** — which already opens the app today. The build to come is **Siri App Intents** (a small piece of native Swift) so Siri can do in-app actions like "mark early text done." Still to scope: which commands first.

The **"Done"** section at the very bottom is just a record of recently finished work, kept for reference — nothing there is waiting on you.

---

## New features (to scope)

- **Timer "nag" styles + loud backup — BUILT in #25, but has a BLOCKING BUG; finish it in #26.** `app/timer.tsx`. Before Start the user picks **Gentle** (every 60s ×3, ~3 min — coffee) or **Urgent** (every 30s ×10, ~5 min — boiling water), plus a **"Loud alert if no response"** switch (one final insistent alert after the nags). Built + `tsc` clean, UNCOMMITTED. **⚠ BLOCKING BUG: tapping the banner / Dismiss does NOT stop the nags on the Simulator** (the on-screen Cancel button DOES). Leading unconfirmed theory: the cancel logic lives in the Timer screen's local state + in-screen listener, so a background/other-screen tap never runs it. **#26 plan:** confirm foreground vs background when tapped → add logging to see if the tap reaches JS → likely move acknowledge/cancel into `app/_layout.tsx` (always mounted) with a **query-based** cancel (`getAllScheduledNotificationsAsync` filtered by `data.timerId`); note timer notifs carry `timerId` but no `source`, so `_layout`'s handler ignores them today. **Two deferred follow-ons:** (1) a genuinely louder/distinct alarm tone needs a **custom sound file** bundled → a REBUILD (currently uses the standard tone); (2) breaking through Silent / Do-Not-Disturb at full volume needs Apple's **Critical Alerts** entitlement (formal application; medical/safety apps) — heavy. iOS limits learned: repeating alerts can't fire faster than once/min and there's a 64-pending cap, so "every 30s forever" isn't possible → urgent is one-shots capped at 5 min. Full detail in handoff "#25".

- **Backup Import — add a MERGE option alongside Replace (Patrick requested 2026-06-27, end of session #24).** Import currently only **replaces** all data. Patrick wants the *choice* to **merge** a backup into what's already on the phone (e.g. importing onto a device that already has data worth keeping). **Its own session** — merge is genuinely more involved than replace because the combine rule differs per data type and each is **Patrick's call**:
  - **ID-keyed arrays** (`todo_tasks`, `vault_items`, `planner_projects`, the routine lists `my_routine`/`week_routine`/`pets_feeds`): add items whose IDs aren't already present; decide the conflict rule when the same ID exists in both (keep current / take backup / keep both as a copy).
  - **Append-only logs** (`my_history`, `week_history`, `pets_history`, `todo_log`, `planner_log`): concatenate then de-duplicate (need a dup key — e.g. date+label+time).
  - **Counters** (`my_coffee`, `my_water`, `pets_treats`): pick higher, sum, or keep current?
  - **Single values** (`user_name`, `reminder_morning_time`, `reminder_evening_time`, `biometric_enabled`, `vault_pin_enabled`, `my_last_date`, `pets_last_date`): backup-wins vs current-wins per field.
  - **Likely UX:** after the file is picked + validated (and Vault decrypted if needed), ask **"Replace or Merge?"**, then run the chosen path. The replace path already exists (`applyRestore` in `app/backup.tsx`); merge would be a sibling function. Scope each rule with Patrick first; build one at a time.

- **Audio input / voice entry — ⏸ PARKED by Patrick 2026-06-26 (session #20).** Hands-free Siri "mark \<item\> done" got all the way to: registration works, tiles appear, **tap works end-to-end**, metadata generates — but Siri won't **VOICE-match** the dynamic parameterized phrase (App-Store fallback). The cheap `DisplayRepresentation` fix (session #20) did not solve it; the live-list requirement rules out `AppEnum`; `IndexedEntity` is an unproven maybe; and an iOS 26.4 App Intents regression suggests part may be Apple-side. **Parked — revisit if a later iOS fixes it, or accept tap-from-Shortcuts (which works).** Re-entry test: run Apple's "BooksShelf" dynamic-entity sample on the device to isolate phone/OS vs. our app. **➤ Full self-contained resume guide: `docs/siri-voice-resume.md` (read it first).** Also: full history below + handoff "#20"/"#19" + `siri-cursor-brief.md` (UPDATE 3). The detailed build history is retained below for reference.
  - **(Pre-park history) SPIKE PROVEN 2026-06-25 (session #15).** Priority is **hands-free**.
  - **Data bridge DECIDED (session #15): Approach B — "Siri drops a note."** Swift can't read AsyncStorage; the App Group is a shared folder both sides see. Rather than mirror the real data into it and duplicate the "mark done" logic in Swift (Approach A, rejected), the Siri intent writes only a tiny instruction (`{action, label, firedAt}`) into the App Group, and the existing RN code applies it on next app-active and clears it. Swift stays a tiny sliver; all real logic stays in JS.
  - **FEASIBILITY SPIKE PASSED (session #15).** Proved a config plugin can inject a compiling, registering Swift App Intent into this **CNG** app (`/ios` is gitignored, regenerated every build — verified `.gitignore` 42–43 — so native code must go through a config plugin). Stage 0 (no build): after `expo prebuild`, the Swift file landed, is in the Xcode Sources build phase, and the App Group entitlement is present. Stage 1 (Simulator, iOS 26.5, no EAS build): app compiled + launched, and "Remember When → Open Remember When" appeared by name in the Shortcuts app. Scaffolding added: `plugins/withSiriIntent.js`, `plugins/ios/OpenRememberWhenIntent.swift` (no-op `openAppWhenRun` intent), `app.json` registration. **Not yet watched: the app foregrounding when the intent runs** — that's the `openAppWhenRun` contract, to confirm on the phone in Stage 2.
  - **Stage 2 — REAL feature BUILT in code (session #16); "Cannot find native module 'AppGroup'" FIXED + Simulator-confirmed (session #17).** Decided: keep display name "Remember When" for now (Elyfont tabled); LIVE item list; My Day only. Built: `modules/app-group/` native bridge (App Group UserDefaults: `setMyDayItems`/`getPendingNote`/`clearPendingNote`), RN wiring (myday.tsx publishes items; _layout.tsx republishes + applies the note on foreground), and `plugins/ios/MarkItemDoneIntent.swift` (intent + `MyDayItemEntity` + `MyDayItemQuery` reading the live list) added to the single AppShortcutsProvider. `withSiriIntent.js` now injects all `.swift`. **THE crash fix (session #17):** the AppGroup podspec required `:ios 16.4` but the app targets `15.1`, so `pod install`'s `supports_platform?` check **silently skipped the pod** (warning, not error) → never linked → "Cannot find native module 'AppGroup'" on Simulator AND on the EAS device build. Lowered the podspec to `:ios 15.1` (removed stray `:tvos`); regenerated → provider + Podfile.lock now include AppGroup; `npm run ios` → app opens clean. (The #16 `package.json` add was necessary but NOT the fix.) App Group capability created in Apple's portal (`group.com.molliedog.ElderlyAssistant` on the App ID).
  - **Stage 2 device test — BLOCKED (session #18): "Mark item done" Siri command won't register on the phone.** Tested on device via TestFlight after two fixes + two EAS builds. Result: the no-parameter **"Open" intent registers** (tile shows in Shortcuts) but the parameterized **"Mark item done" intent does NOT** — no tile, and Siri says "I don't see the app you asked for." (Reasoning note: "Hey Siri, open Remember When" working is NOT evidence — iOS opens any app by name natively; only the Shortcuts *tile* proves our intent registered.) Fixes tried, both committed, neither sufficient: (1) `EntityStringQuery` + `entities(matching:)` on `MyDayItemQuery` (`ffc2f27`); (2) launch-time `RememberWhenShortcuts.updateAppShortcutParameters()` injected into `AppDelegate` via a new `withShortcutRefresh` mod in `withSiriIntent.js` (`f57def6`). Delete+reinstall didn't help; Simulator can't validate (no App Group provisioning, can't run shortcuts). **NEXT (next session):** phone **reboot** first (free — Siri vocabulary can need it). Then the decisive **diagnostic build** — hardcode `suggestedEntities()`/`entities(matching:)` to a static list (no App Group): if the command then appears → it's the **App Group read on device** (provisioning/entitlement; also delete the stray duplicate `group.group.com.molliedog.ElderlyAssistant`); if not → **structural** App-Shortcut rejection (rework the dynamic-entity approach). Full detail in handoff "THIS SESSION — #18".
  - **Stage 2 device test — REGISTRATION SOLVED, but VOICE BLOCKED (session #19, 2026-06-26).** A phone **REBOOT** cleared the #18 blocker: the per-item "Mark item done" tiles now appear in Shortcuts and survive reboot, and **tap works end-to-end** (marks done + logs). So registration, App Group read, intent, app-wake, and RN logic ALL work on device — both #18 hypotheses (App-Group-read / structural) are disproven. **The real, isolated blocker: Siri won't VOICE-match the parameterized command's DYNAMIC `AppEntity` item parameter** — perfect transcription, but "I don't see an app / Search the App Store." Tried renaming the app to **"Elyfont"** (theory: the English-phrase name "Remember When" mis-segments the spoken phrase) — **built + device-tested, did NOT fix voice**, disproving that theory (a distinctive coined name fails identically; "open Elyfont" works, "mark snack done with/in Elyfont" doesn't). **Three options remain — next session picks via Cursor:** (1) `IndexedEntity` + Spotlight donation (keep live list; iOS 18+), (2) fixed `AppEnum` (lose live list, but Apple's documented voice-matchable pattern — likeliest to work), (3) **native-Swift rebuild** (app is iOS-only; native makes App Intents first-class — cheap proof = a tiny native Swift Shopping List app testing dynamic-list voice in a clean project). **NOTE this supersedes the #14 "do NOT rewrite in Swift" decision below — a native rewrite is now genuinely on the table.** See handoff "THIS SESSION — #19" + `docs/siri-cursor-brief.md` (esp. "UPDATE 2").
  - **Original #14 decisions (mostly unchanged; the "don't rewrite in Swift" one is now reopened — see #19 above):**
  - **Custom in-app mic button: DROPPED.** iOS keyboard dictation already does voice-to-text in any field; a custom button would just duplicate it.
  - **Custom always-listening wake word ("Remember When, Open") inside our app: RULED OUT (verified).** Apple reserves background always-on mic for Siri only (battery + privacy); a third-party app can only listen while open and on-screen.
  - **Hands-free path = SIRI.** "Hey Siri / side-button → open Remember When" already opens the app today, no code (confirmed live). Enable Settings → Siri voice activation for fully button-free.
  - **Build goal = Siri App Intents.** A small native-Swift slice (Expo config plugin / local module + App Group shared container) so Siri can run in-app actions: "mark early text done in Remember When," "add a task …". Heavier native lift; its own scoping session + its own build.
  - **Feasibility facts (don't re-derive):** in-app STT = `expo-speech-recognition` (jamsch, SDK-54 build, wraps `SFSpeechRecognizer`, config plugin, needs dev/EAS build, mic+speech perms); read-aloud = `expo-speech` (official, trivial); App Intents = pure Swift (iOS 16+), needs the native bridge, community helper `@config-plugins/react-native-siri-shortcut` exists.
  - **Native-Swift pros/cons (decided):** only route to App Intents, best reliability — but Swift is new to Patrick, separate native piece + bridge, Xcode debugging, API churn. **Agreed: do NOT rewrite the app; add only a walled-off Swift slice for Siri, keep the rest in React Native.**
  - **Open to scope next session:** which command(s) first (likely "open" + one action), how a spoken phrase names an item, and a small feasibility spike before a full build. One change at a time.

## Bugs / correctness

- **SECURITY — Vault protection could be removed without auth — ✅ FIXED + SIMULATOR-TESTED 2026-06-27 (session #22).** Was: `toggleVaultPin` (`app/settings.tsx` ~126) wrote `vault_pin_enabled` with NO auth check, so anyone holding the phone could open Settings, flip "Extra Vault Security" off, and read the Vault (the app opens straight to `/home`, no launch PIN). **Fix:** turning the toggle OFF now requires Face ID / passcode (`authenticateAsync`; on failure it returns early so the Switch snaps back ON); turning it ON stays free. Committed `a8511c3`. Verified on the iPhone 17 Simulator with simulated Face ID. See handoff "#22".
- **SECURITY — forgotten PIN has no recovery (found #21).** Decision FIRM: **retire the custom 6-digit PIN, use Face ID / passcode** (Apple handles recovery — never locked out). **Patrick does NOT remember the PIN and NO data is at risk**, so no recovery path needs building. **Done so far (session #22):** the Vault gate is now Face ID (`app/vault.tsx`) and its re-prompt **loop was fixed + Simulator-tested** (committed `3bd445e` — note that commit's message is mislabeled "settings.tsx toggle fix" but it contains vault.tsx; the gate now runs once on open). **Still open (non-exposure cleanup):** `Reset All Data` (`settings.tsx` ~192) still demands `user_pin` → move to Face ID; remove the dead **Change PIN** row + keypad. See handoff "#22".
- **SECURITY cleanup — orphaned PIN screens + stale toggle hint (verified session #22).** Grep confirms NOTHING navigates to `app/vaultpin.tsx` or `app/login.tsx`; `app/setup-pin.tsx` is reached ONLY via Reset All Data (`settings.tsx` ~210). All three are still registered in `app/_layout.tsx`. Once the PIN is retired, decide delete vs keep. Also: ✅ the stale Vault toggle hint "Require PIN to open Vault" was reworded to "Require Face ID to open Vault" (session #24 bug-fix); the orphaned-screen decision is still open.
- **Timer cancel silently failed — ✅ FIXED + DEVICE-VALIDATED + COMMITTED (session #25)** (`app/timer.tsx`). Confirmed real: `cancelTimer` used `timer.id` (a `Date.now()` string) instead of the identifier returned by `scheduleNotificationAsync`, so the main "Timer Done" alert was never cancelled (the follow-up's real id WAS saved, so only it got cancelled). Fix: store the main alert's real id and cancel with it. Patrick validated on Simulator + phone. Committed. ⚠ Superseded in the working tree by the nag-feature rewrite (`notifId` → `notifIds[]`).
- **To-Do "recurring" — Weekly/Monthly/Yearly now schedule; only every3months/every6months remain** (`scheduleReminders`, `app/todo.tsx`). WEEKLY (session 6), MONTHLY (`day recurDay`), and YEARLY (`month recurMonth−1` [Expo 0-based], `day recurDay`) branches all fire native repeating triggers at `dueTime` with the `todosnooze` banner (session #8 added monthly/yearly — committed, device test pending). **every3months / every6months build nothing**: no native trigger AND no anchor-date picker in the form. Approach to design later: add a "starting date" picker, then either reschedule-on-fire (only runs when the app is opened/tapped — shaky for a memory aid) or pre-schedule the next few one-shots and top up on app open (sounder; respect the iOS 64-pending cap).
- **My Day after-midnight doses logged on the wrong day — FIXED + DEVICE-VALIDATED 2026-06-25 (session #15).** Was: banner **Done** (`_layout.tsx` `action === 'done'`, non-To-Do branch) only set `completed: true` and wrote **no** `my_history` entry; the daily reset (`loadData`, `myday.tsx` ~line 119) then cleared the flag → the dose vanished. (On-screen **Log**, `confirmLog` ~line 204, did write history but dated from `new Date()` at tap-time → wrong after midnight.) **Fix:** the banner Done branch now writes a dated history entry to `my_history` (and `pets_history` for Pets), date+time from `response.notification.date`×1000 — the moment the reminder FIRED, not the tap moment, so a late-night dose files under the reminder's day. iOS reports `notification.date` in SECONDS (verified `EXNotificationSerializer.m`), hence ×1000. **✅ Phone test 2026-06-25:** Medication fired 11:55 PM 06/24, Done after midnight → Log `06/24 | 23:55 | Medication`. **Pets half (`pets_history`) same code path, not separately device-tested.** **Not changed:** on-screen Log still uses tap-time (deferred; would need a cutoff-hour rule). **Edge:** a Snooze crossing midnight files under the new day.
- **To-Do "Done" logs tap-date, not the reminder's date — ✅ FIXED in code (session #25), UNCOMMITTED + NOT device-tested** (`app/_layout.tsx`, `action === 'done'` → `source === 'todo'` branch). Was: `completedDate` built from `new Date()` at tap time, so a Done tapped on a stale banner recorded **today**. Fix simpler than the originally-sketched "carry the fire date in `data`" idea — just stamp from `new Date(response.notification.date * 1000)` (the fire time), matching the My Day / My Week / Pets paths already in the file; no `todo.tsx` edit needed. The on-screen Done in `todo.tsx` keeps tap-time (no fired notification), same as My Day's accepted limitation. **Caveat:** for dated tasks with an EARLY reminder ("Week/Month before"), Done logs the fire day, not the due day; fine for the recurring "Day" reminders where the bug was seen. Still needs a phone test.
- **Yearly day picker allows invalid dates — ✅ FIXED (session #24 bug-fix).** Added `DAYS_IN_MONTH`; the Yearly Day list is now driven by the selected month's length, and switching to a shorter month clamps the chosen day. Feb 29 is still selectable on purpose (Feb's real max). (`app/todo.tsx`.)
- **My Week on-screen "Done" stamps tap-time date, not the chore's due date** (`app/myweek.tsx`, `confirmLog`). Minor, and identical to My Day's accepted limitation: the in-app Done (Log modal) dates the history entry from `new Date()` at the tap moment. The *banner* Done is correct (it uses the fired time). Only matters if he logs a chore in the app well after its day. Low priority; same fix shape as the My Day on-screen Log would need (a cutoff-hour rule). Built session #12.
- **Old `pets_data` storage key is orphaned — ✅ FIXED (session #24 bug-fix).** Grep confirmed `pets_data` is read/written nowhere in `app/`; added a one-time `AsyncStorage.removeItem('pets_data')` at the top of `loadData` (no-op if absent). (`app/mollie.tsx`.)

## Design decisions

- **Where do daily-repeating reminders live? → DECIDED 2026-06-19.** My Day is the daily engine; To-Do drops "Daily" entirely. Also decided: the "Weekly" category no longer requires a due date. (Weekly/Monthly/Yearly now schedule recurring notifications as of session #8; only 3/6-month remain — see Bugs/correctness.)
- **Pets Day routine reminders → DONE (2026-06-17).** Built in session 4: daily "Pets Routine" reminders with Snooze 15/30/60, touching `mollie.tsx` + `_layout.tsx`. Committed and device-validated on build 18. (See Done section.)
- **Tap-routing is screen-only and untested** (`app/_layout.tsx`). Routes by `data.source` to the right SCREEN but never lands on the specific item, and has never been confirmed with a real tap. To-Do reminders carry `taskId` (landing on the task is feasible); My Day and To-Do "Background" reminders carry no item id. (If Pets Day notifications land, decide its `data.source` routing too.)

## UI polish

- **Two "+Task" buttons — ✅ FIXED (session #24 bug-fix).** To-Do had a header "New Task" and a bottom floating "+ Task" opening the same modal; removed the floating one, kept the header button. Simulator-eyeballed. (`app/todo.tsx`.)
- **Vault "+ Add" should move to the header — ✅ FIXED (session #24 bug-fix).** Moved from a bottom floating button to the header top-right (shown when inside a category). Simulator-eyeballed. (`app/vault.tsx`.)
- **Backup filename is date-only — ✅ FIXED (session #24 bug-fix).** Filename stamp is now `YYYY-MM-DD-HHMM` (e.g. `Elyfont-Backup-2026-06-27-1604.json`), so same-day re-exports keep distinct names. Simulator-eyeballed. (`app/backup.tsx`.)
- **Project Planner schedules nothing** (`app/planner.tsx`). Has reminder UI/fields but wires up no notifications. Dormant, low priority.

- **App renamed to "Elyfont" — finish the naming consolidation (session #19, 2026-06-26).** The display name (home-screen label + the name Siri reads) is now **Elyfont**; bundle id + App Group stay `…ElderlyAssistant` on purpose. Three loose ends, all cosmetic / non-Siri:
  - **In-app home-screen header still says "Remember When"** ("Good to see you…") — hardcoded greeting text in the home screen code, not the app name. Update to "Elyfont" when convenient.
  - **TestFlight / App Store Connect listing name still "Remember When"** — a manual App Store Connect setting, separate from the build. Rename there if/when wanted.
  - **Tagline "Elyfont – Memory Assist" (Patrick wants this).** Must NOT go in the app name — it would truncate on the home screen AND re-introduce the English-words-in-the-name Siri risk. Put it as an in-app subtitle on the home screen (big "Elyfont", "Memory Assist" beneath) and/or the App Store **subtitle** field.

## Done (recently cleared from this list)

- **Monthly + Yearly recurring To-Do alerts (Group 1, the scoped half)** — DONE 2026-06-19 (session #8), **committed, device test pending**. `scheduleReminders` got native repeating MONTHLY (`day recurDay`) and YEARLY (`month recurMonth−1`, `day recurDay`) branches at `dueTime`, mirroring weekly, with the Done/Snooze/OK banner. Verified the library supports these triggers and that Expo months are 0-based. Use cases: Monthly = bills; Yearly = annual physical / furnace filter / smoke-detector batteries. **3-month + 6-month stay parked** (above), plus the Yearly-day-picker edge case (above). (`app/todo.tsx`.)
- **Appointment "Reminder Options" (the original purpose of the app)** — DONE 2026-06-19, **committed, awaiting build + device test**. Editable global **Morning (8 AM) / Evening (5 PM)** times in Settings (`reminder_morning_time`/`reminder_evening_time`); seven toggle-highlight **"Reminders before"** buttons in To-Do (At time / 1 hour / 2 hours / Morning of / Day / Week / Month) — "Morning of" fires the morning time on the appointment day, "Day/Week/Month" fire the evening time 1/7/30 days before, the rest are offsets from the appointment time; silent **OK** dismiss added to the To-Do banner. (`settings.tsx` + `todo.tsx` + `_layout.tsx`.) Note: only applies to *dated* To-Dos; weekly/monthly/etc. recurring scheduling is unchanged. Per-appointment time override parked above.
- **To-Do: remove Daily, Weekly works with no date, Done + Snooze banner** — DONE in working tree 2026-06-19 (session 6), **awaiting commit + device test**. Removed 'Daily' from the recurring picker; Weekly To-Dos now show day+time and fire a repeating WEEKLY alert with no date; To-Do reminders got a `todosnooze` category (Done + Snooze 15/30/60). Done logs to history and keeps repeating tasks running; one-time tasks are removed + cancelled. (`app/todo.tsx` + `app/_layout.tsx`.) Remaining spin-offs parked above: schedule monthly/3mo/6mo/yearly, and an optional on-tile Snooze button.
- **Notification "Done" action + on-page Snooze (My Day + Pets Day)** — DONE 2026-06-19 (session 5). Added a **Done** button to the `mydaysnooze`/`petssnooze` banner categories (marks the item complete in `my_routine`/`pets_feeds` by `itemId` and cancels the fired notif, in `app/_layout.tsx`), and an on-page **Snooze** button on every My Day + Pets Day tile that pops up 15/30/60 (`app/myday.tsx` + `app/mollie.tsx`, reusing the TIME_INTERVAL snooze scheme). **Committed + device-validated.** (To-Do equivalent deferred → now the next goal.)
- **Pets Day routine reminders (with Snooze)** — DONE 2026-06-17 (session 4). Pets Day had no notification code; now mirrors My Day. `mollie.tsx` got permission + handler on mount and `scheduleAllPetsNotifications` (DAILY trigger per incomplete feed, title "Pets Routine", `petssnooze` category); `_layout.tsx` got the `petssnooze` category, source-based snooze branching, and `pets`/`petssnooze` → `/mollie` routing. **Committed + device-validated on build 18.**
- **Counters reset on page return (My Day + Pets Day)** — DONE 2026-06-17 (session 4). Coffee/Water/Treats were in-memory only and reset to 0 whenever you left and came back. Now stored in AsyncStorage (`my_coffee`, `my_water`, `pets_treats`), loaded on open, saved on every +/−, and reset on the existing daily rollover. **Committed + device-validated on build 18.**
- **To-Do tiles show the due TIME** — DONE 2026-06-15 (session 2). Both render spots (`app/todo.tsx` lines 515 + 554) now append `' at ' + task.dueTime` when set, shown exactly as typed (free-text field), matching the reminder body. **Committed 2026-06-15 (`d7b4e81`); awaiting device test.**
- **`deleteTask` cancels reminders** — DONE 2026-06-15 (session 2). Added `cancelReminders(id)` in the Delete handler (`app/todo.tsx` ~line 239), mirroring `completeTask`. **Committed 2026-06-15 (`d7b4e81`); awaiting device test.**
- **`settings.tsx:165` TS error** — DONE 2026-06-15. Final fix is `onPress: async (pin?: string)`. (Session 2's first attempt used `pin: string`, which itself caused TS2322 — `Alert.prompt`'s callback value is `string | undefined`, so `string` was too narrow and blocked the commit. Session 3 corrected it to the optional `pin?: string`.) **Committed 2026-06-15 (`d7b4e81`); awaiting device test.**
- **Merge My Day Meals + Meds into one card-styled list** — DONE 2026-06-15. Now one "Routine" list of Entries under `my_routine` (migrated from `my_schedule` + `my_meds`), single common Log, "+ Add Entry" in header.
- **My Day tile time formatting (AM/PM)** — DONE 2026-06-15. `format12Hour` now outputs real 12-hour AM/PM on tiles, and the time picker is Hour(1–12)/Minute/AM-PM on both My Day and Pets Day.
