PENDING WORK — plain-language list
Last updated: 2026-07-02 (session #50 — Project Planner now has both
themes; To-Do got them in #49. Nine pages done, four to go: Look Ahead,
My Day, My Week, Pets. Full rollout order is in parked-items.md under
"TWO-THEME ROLLOUT.")

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- The app ("Remember When") is stable and on your phone via TestFlight.
  Nothing is half-built or broken in a way that's holding you up.
- Sessions #49 + #50: To-Do and Project Planner both follow both themes
  now. Planner's priority colors (red/gold bars) and status words
  (Active teal, On Hold orange) come from the theme. You checked
  Planner in the Simulator — both themes, popups too — and approved.
- Also fixed in #50: the dark-theme Home greeting ("Good to see you,
  Patrick!") wrapped to two crooked lines on your phone. Now it sits
  identically in both themes — nothing moves when you switch.
- Your calls this session: Planner's title is now 24 (26 crowded the
  Home pill — and titles don't NEED to match at 26; fitting well
  matters more). And the BIG one: NO more TestFlight builds until the
  last four pages are converted, then ONE build picks everything up —
  each build past your monthly $45 Expo credit costs real money
  (last period's 22 builds used the credit exactly). You handle the
  Expo billing side yourself.
- Worth knowing: nine converted pages obey the theme choice; the four
  unconverted ones (Look Ahead, My Day, My Week, Pets) stay light
  until their sessions come. The dark startup flash is still there.
- Your phone's TestFlight build is from before #49, so it shows none
  of this yet — that's expected until the one batched build.
- Not yet committed — you commit this session's code + docs when ready.
- Next session (#51): lookahead.tsx. Then My Day (the biggest), then
  My Week + Pets together (near-twins, fastest). Then the batched
  build and the phone checkpoints.


YOUR #41 LIST — WHAT'S STILL OPEN (items 2, 5, 6, 7 done in #42)
----------------------------------------------------------------------------
1. Name the backup folder — moved up from "someday" to "wanted." Best
   finished and checked on the real phone, where iCloud Drive is involved.
   (Now related: the app-name change back to "Remember When" touches the
   same backup file naming.)
3. Import files/documents into the Vault. Needs a talk-through first
   (what kinds of files, from where, how they're shown and protected).
4. My Day: a banner "Done" on YESTERDAY'S reminder checked off TODAY'S.
   Most likely your phone is still running the older build from before the
   fix (#39) — the fix just hasn't been phone-tested yet. You're tracking
   whether it happens again; it's on the structured-test checklist.


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
- The new theme switcher and popup choice (#48) — Simulator-approved;
  give them a once-over on the real phone after the next TestFlight build.
- A To-Do reminder that seemed to fire early. Checking the code found no
  bug — almost certainly the "2 hours before" option doing its job. To be
  sure: make a task due a few minutes out, pick ONLY a short preset, and
  watch if it fires on the minute.
- The buttonless To-Do banner (#40), the shared routine popup (#39), Look
  Ahead's reminders (#37), a Watch List once-over (#41), and the #42
  once-over (To-Do without categories, Look Ahead popup buttons, the
  My Day / Pets un-check) — all batched into the phone checkpoints.


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
- Backups skip some data (found #41, grew #48). The backup code has a
  fixed list of what to save; Look Ahead, Watch List, AND now the theme +
  popup choices were never added. Until fixed, restoring a backup loses
  those. Simulator-testable — a good small session.
- The Vault "Custom" label suspicion (spotted #47, NOT yet confirmed):
  typing your own label under "Custom" may save the word "Custom"
  instead. Test steps are in parked-items.md; if real, the fix is tiny.
- My Week: marking a chore done from inside the app records today's date
  instead of the chore's own day. Minor.
- Tapping a reminder takes you to the right screen but not to the exact
  item, and this has never actually been tried with a real tap.


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Backup "Merge" option. Restoring currently REPLACES everything; you
  wanted the choice to MERGE instead. Its own session — each kind of data
  combines by its own rule, and those are your calls.
- Vault import (your item 3 above) — scope it out before building.
- Remove categories from Vault (your #47 idea) — talk-through first,
  like #42 did for To-Do.


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- Rename the app back to "Remember When" (#48, yours). The icon label
  and the backup file naming still say "Elyfont"; the home greeting and
  App Store listing already say "Remember When." Needs a rebuild to show
  on the phone; old backups must stay restorable. The "Memory Assist"
  tagline still needs a home.
- Project Planner has reminder fields on screen, but they aren't
  connected to anything yet.
- Make the button labels match: To-Do says "New Task" while Vault says
  "+ Add."
- Add a Snooze button on each To-Do item itself. My Day and Pets Day
  already have one on every tile; To-Do only has it on the banner.


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Voice control through Siri. Set aside after much effort; revisit only
  if a future iPhone update fixes the name-matching. (The app-name revert
  above assumes this stays shelved.)
- The Timer "Loud alert" isn't actually louder — a genuinely louder alarm
  needs a custom sound built into the app and special Apple permission to
  break through Silent / Do-Not-Disturb. Heavy — deferred.
- A running Timer's tile disappears if the app reloads or restarts (the
  list of active timers is only held in memory). Accepted as-is: "Done"
  still stops the alerts either way.
- Reminder times stay global — already decided, nothing to do.
