PENDING WORK — plain-language list
Last updated: 2026-07-08 (session #67 — TWO things: the MEMORY
TEST was confirmed done and on the phone, working just as you
want (you speak the words in with the mic now). Then the VAULT
got its makeover: ALL categories are yours now — add with
"+ New", rename with Edit on each tile, delete with a swipe and
a double warning. Simulator-approved; waits on commit + build.)

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- NEW THIS SESSION (#67): THE VAULT CATEGORIES ARE ALL YOURS.
  No more fixed list — the app starts you off with the same
  seven you had (everything stays where it was), and from now
  on you add your own with "+ New", rename any with its Edit
  button, and delete with a swipe — two warnings first, and the
  items inside go with it (fine: the Vault is a pocket
  reference; the originals live elsewhere). Icons are gone from
  category tiles — clean names only. Adding an item is now just
  type-the-label — no more preset chips, and that long-suspected
  "Custom" bug (your typed label saved as the word "Custom")
  turned out to be REAL and is now gone with the chips. Your
  categories ride backups. SIMULATOR-APPROVED, both themes —
  "everything looks good." AWAITING YOUR COMMIT + BUILD.
- THE MEMORY TEST IS DONE AND ON THE PHONE (#66 built it, #67
  confirmed it): five practice rounds, the 5-minute recall
  notification, one test a day, scores saved with dates for
  tracking the PAP mask's benefit. WORKS JUST AS YOU WANT — you
  speak the words in with the keyboard mic now.
- THE RENAME IS BUILT (#65), all of it: "Remember" under the
  icon, the happy-face icon picture, "A Place To Remember" on
  the Home page (with a small face icon next to your greeting)
  and on the Settings version line, and backups now save as
  "Remember-Backup-<date>.json" — OLD backups still restore.
  You also changed the store listing name to "A Place To
  Remember" in App Store Connect yourself, live in the session.
- BUILD 48 IS ON THE PHONE — committed, built, and submitted the
  same day. Getting it to Apple took three tries: an expired
  Apple developer agreement, then the EU "Digital Services Act"
  question (you answered "not a trader"), both fixed in App
  Store Connect under Business. If a future submit fails with
  "agreement missing or expired" — that's the place to look.
- ALL THE VISUAL CHANGES ARE IN AND APPROVED GOOD (your report,
  #67) — the light-theme header face included. Nothing visual
  left to check.
- THE #63 ORDERS TEST IS DONE — verified working on the phone
  (your report, #67). The whole Orders page is proven.
- OLD BACKUPS ARE A CLOSED BOOK: you exported fresh backups
  under the new Remember-Backup name and deleted the old
  Elyfont files. No old-file restore proof needed. (The code
  still accepts old backups; it just never needs to.)
- STILL TO COMMIT: just these docs (Patrick commits).
- The dark startup flash is still there (a known small quirk).


WHAT'S NEXT
------------------------------------------------------------------
1. COMMIT the Vault work + these docs, and BUILD. On the phone:
   your items should be right where they were under the same
   seven names (no icons now); add a category, rename one,
   delete one (two warnings, its items go too); add an item and
   check it saves the label YOU TYPED (the old "Custom" bug,
   proven dead on the phone); export a backup and see
   vault_categories in it; the "← All Categories" capsule.
2. THE ROADMAP.MD REFRESH — started this session, parked when
   you chose the Vault build instead. It still shows the #31
   picture (old Elyfont name, four newer pages missing from the
   built list, phases long overtaken). Natural next-session
   pick, and the "where does the app go from here" talk comes
   with it.
   INCLUDE IN THE REWRITE (your ask, #67): a DOCUMENTATION
   milestone — a build document for THIS app, done the way
   MysteryTracker's spec was (Claude read it this session):
   plain-English, function-anchored, detailed enough to rebuild
   the app from the document alone; possibly a companion
   draw.io diagram as its map; and a style-rules file to govern
   how it's written, page by page, one session-goal at a time.
3. After that it's an open pick — Vault file import needs its
   talk-through (DECISIONS below), and the smaller items live in
   the lists that follow.


#40 & #41 STILL OPEN (what's left of your #41 list)
----------------------------------------------------------------------------
(Item 1 — backup naming/findability — is CLOSED: the filename is
Remember-Backup-<date>, and you've exported and managed the new
files yourself, #67.)
3. Import files/documents into the Vault. Needs a talk-through first
   (what kinds of files, from where, how they're shown and protected).


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
- THE VAULT CATEGORY PACKAGE (#67) — the checklist is item 1 of
  WHAT'S NEXT above. Simulator-approved; the phone proves it.


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
- Look Ahead: delaying from the BANNER doesn't show the "▶ Delayed"
  line on the tile (your #61-build phone test). Reading the code
  found the likely cause: the page only re-reads its saved items
  when it first opens, so a banner delay done while the page was
  already loaded never appears — and can even be lost. The Delay
  button ON the tile is fine. Fix is small (re-read on every visit).
  Also checked: a Delay is NOT supposed to write a log entry —
  only Done logs. That part is by design.
- My Week: marking a chore done from inside the app records today's date
  instead of the chore's own day. Minor.
- Tapping a reminder lands on the right page (tested #55), but taking
  you to the exact item on that page is still not built. Minor.


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Vault import (your item 3 above) — scope it out before building.
  (The Vault category work is BUILT — #67. Nothing left to decide
  there.)


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- Watch List overhaul — its own session, talk-through first (moved
  from What's Next, your call #55). The night-and-time-a-TV-show-airs
  idea folds into it.
- (The app rename is DONE — #65.) The "Memory Assist" tagline
  still needs a home — the App Store subtitle field is empty and
  is one natural spot.
- The Android icon pictures and the splash (startup) image still
  carry the old placeholder art — deliberately left for later
  when the main icon got the new face (#65).
- Project Planner has reminder fields on screen, but they aren't
  connected to anything yet.
- My Day / Pets: a Done tapped from the BANNER logs only the fire
  ("set") time. You want the entry to carry both times like the
  in-app logs do — the set time AND the time Done was actually
  tapped (#55).


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Moved here from What's Next (your call, #55): the structured
  reminder test checklist; My Week's done-date recording; exact-item
  tap routing; the dark startup flash; Planner's unconnected
  reminder fields. (Retiring Colors.ts: DONE in #57.)
- Voice control through Siri. Set aside after much effort; revisit only
  if a future iPhone update fixes the name-matching. (The app-name revert
  above assumes this stays shelved.)
- The Timer "Loud alert" isn't actually louder — a genuinely louder alarm
  needs a custom sound built into the app and special Apple permission to
  break through Silent / Do-Not-Disturb. Heavy — deferred.
- Backup "Merge" option — BACK-MOST BURNER (#67): Patrick is 90%
  sure he's dropping it. Restore keeps REPLACING everything, which
  works fine for his one-device use. Only revisit if he raises it.
