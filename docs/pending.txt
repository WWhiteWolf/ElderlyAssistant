PENDING WORK — plain-language list
Last updated: 2026-07-01 (session #39 — Step 4 (routine half): My Day, My Week and
Pets now share ONE pop-up — OK / Skip / Delay 15/30/60 / Done. Snoozed reminders got
their sound back; an old pop-up from a past day no longer checks off today's item.
Next session: build a structured test plan for ALL the reminders.)

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- The app ("Remember When") is stable and on your phone via TestFlight.
  Nothing is half-built or broken in a way that's holding you up.
- This session (#39) finished Step 4: the routine half. My Day, My Week and
  Pets now all show the SAME pop-up when a reminder fires, with six buttons:
    * OK — dismiss just this pop-up, nothing else changes.
    * Skip — skip this one occurrence: it stops nagging today, nothing is
      marked done or logged, and it comes back on its normal schedule.
    * Delay 15 / 30 / 60 min — snooze just this one reminder.
    * Done — check off and log. NEW GUARD: if the pop-up is left over from a
      past day, it logs that old completion but no longer checks off today's.
  Also fixed: snoozed reminders from the banner were re-firing silently — they
  have their sound back. My Week's pop-up lost its "+1 Day" button (your call);
  postponing still works from the page itself.
  All three pages checked in the Simulator (screenshots). What the Simulator
  can't prove — sound, the past-day guard, real lock-screen behavior — waits
  for the phone.
- All of session #39 (code + these docs) is ready for you to commit.
- The plan, in a nutshell: (1) ✅ DONE — repeating options out of To-Do; (2) ✅
  DONE — build "Look Ahead"; (3) ✅ DONE — Look Ahead reminders + Delay; (4) ✅
  DONE — same pop-up everywhere (To-Do its own way in #38, the three routine
  pages sharing one in #39); (5) fold in your "Watch List" app as another page.
- What's next — YOUR DECLARED PICK: a session devoted to STRUCTURED REMINDER
  TESTS. There are too many reminder kinds to keep track of by memory, so
  we'll build one organized checklist (every page, every button, every rule)
  and work through it — Simulator first, with the phone-only parts batched
  into the cloud-build checkpoints. Nothing here is urgent.


BIG PLANNED WORK (the #34 plan — ONE effort, pages first, Simulator-first)
----------------------------------------------------------------------------
These are kept together as one initiative ("Reminder Rebuild #34") so they get
done as a set and don't get lost among other items. As much as practical is
built and checked in the Simulator first (free) before spending a phone build;
the real-phone tests are batched into two checkpoints. Full details + the
ordered build checklist live in docs/reminder-audit.md. The four pieces:
- [DONE #35] Strip the repeating options out of To-Do (Monthly, Yearly, 3 & 6
  months) so To-Do is just one-time tasks. Done, Simulator-checked.
- [DONE #36] Build the new "Look Ahead" page (home screen, like My Day): items
  grouped Monthly / 3 Months / 6 Months / Yearly, each with first-due date, time,
  repeat choice, and its own log. Done, Simulator-checked.
- [DONE #37] Give Look Ahead its reminders: each item nags on schedule and rolls
  forward to its next date when you mark it done; Delay choices of Day / Week /
  Month (on the pop-up and as an on-tile button), with a "▶ Delayed …" line.
  Done, Simulator-checked. The real-phone test for this is Checkpoint A (below),
  still owed.
- [DONE #38 + #39] Make the reminder popups behave consistently. To-Do stands on
  its own with a simple OK + Done popup (#38) — a To-Do is a fixed one-time
  appointment, not a repeating routine. My Day, My Week and Pets share one popup
  with OK / Skip / Delay 15/30/60 / Done (#39), with the past-day guard so an old
  popup can't check off today's item. Timer left out on purpose. Done,
  Simulator-checked; the real-phone confirmation is batched into the checkpoints.
- NEXT (your pick, #39): STRUCTURED REMINDER TESTS. One organized checklist
  covering every reminder kind and button, worked through methodically —
  Simulator first, phone-only parts batched into the cloud-build checkpoints.
- Fold your "Watch List" movie/TV app in as another home-screen page (it's
  built already, in its own folder). Separate from the reminder work.


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
- A To-Do reminder that seemed to fire early. You recall setting one for
  23:30 and it went off around 21:15. Checking the code found no bug — that's
  exactly 2 hours before a 23:15 time, so it was most likely the "2 hours"
  before-reminder option doing its job. To be sure: make a new task due a few
  minutes out, pick ONLY "At time," and watch if it fires on the minute.


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
- To-Do "Custom Category" cancel wipes the new task. When adding a New Task,
  if you open the Custom Category second popup and tap Cancel, BOTH popups
  close and the task you were entering is lost. Cancel should only back out of
  the category popup and return you to the task you were filling in.
- (DONE — folded into the #34 plan) The old "3 Months" / "6 Months" To-Do options
  that did nothing are gone: all the repeating options moved out to the Look Ahead
  page, which now handles them with real reminders (finished in #37).
- My Week: if you mark a chore done from inside the app (not from the
  notification), it records today's date instead of the chore's own day.
  Minor — only matters if you log a chore well after its day.
- Tapping a reminder takes you to the right screen but not to the exact
  item, and this has never actually been tried with a real tap.


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Backup "Merge" option. Right now restoring a backup REPLACES everything on
  the phone. You wanted the choice to MERGE a backup into what's already
  there instead. That needs its own session because each kind of data
  (tasks, logs, counters, settings) has to combine by its own rule, and
  those are your calls.


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- Name the backup folder. Give the exported backup folder/file a clear name
  so it's easy to spot wherever it's saved (e.g. iCloud Drive).
- Project Planner has reminder fields on screen, but they aren't connected
  to anything yet.
- Finish the "Elyfont" renaming: the in-app home greeting still says
  "Remember When"; the TestFlight / App Store listing is still named
  "Remember When"; and the "Memory Assist" tagline still needs a home
  (it can't go in the app's name).
- Make the button labels match: To-Do says "New Task" while Vault says
  "+ Add."
- Add a Snooze button on each To-Do item itself. My Day and Pets Day already
  have one on every tile; To-Do only has Snooze on the notification banner.


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Voice control through Siri. It gets you to the app and tapping works, but
  Siri won't recognize spoken item names ("mark snack done"). After a lot of
  effort this was set aside. Revisit if a future iPhone update fixes it.
- The Timer "Loud alert" isn't actually louder — it's the normal alert tone.
  A genuinely louder, distinct alarm needs a custom sound file built into the
  app (a full rebuild), and breaking through Silent / Do-Not-Disturb needs a
  special Apple permission meant for medical/safety apps. Heavy — deferred.
- A running Timer's tile disappears if the app reloads or restarts (the list
  of active timers is only held in memory). Accepted as-is: tapping "Done"
  still stops the alerts whether or not the tile is showing.
- Reminder times stay global — already decided, nothing to do. The Morning /
  Midday / Evening times are set once in Settings and apply to everything.
  You considered letting a single appointment use its own times and chose
  not to. This note only exists in case you ever want to revisit that.
