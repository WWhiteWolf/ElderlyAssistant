PENDING WORK — plain-language list
Last updated: 2026-07-05 (session #63 — the ORDERS PAGE IS BUILT,
all of it in one session: the tile, the form, the HERE button and
arrivals log, the reminders, and the banner buttons. Everything
checked out in the Simulator, including a live banner with the
HERE / OK / Delay buttons. The phone test rides your next build.)

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- THE ORDERS PAGE IS BUILT (#63). The 📦 tile is on the Home page
  (11th button). One entry per item: name, price, store, address,
  order number, the "expected by" date, and a time window you add
  later by hand when the store narrows it down. HERE logs the
  arrival and clears the entry; the Arrivals Log sits below the
  list. Reminders: day before (at your Midday time), morning of
  (at your Morning time), window open, and window close unless
  you've already tapped HERE. Banner buttons: HERE / OK / Delay
  15-30-60.
- Orders data is included in Backup from day one.
- Everything was checked in the Simulator step by step, including
  a live banner firing with the right buttons on it.
- All #63 CODE is committed and on your phone (two builds the same
  day: the Orders build, then the bridge rollout + tap-area build).
  Only the DOCS still need their commit — the usual post-test
  docs commit closes out the session.
- The dark startup flash is still there (a known small quirk).


WHAT'S NEXT
------------------------------------------------------------------
1. COMMIT + BUILD + PHONE TEST. The new build carries the whole
   Orders page AND the #62 fixes — the phone checklist is in the
   NEEDS A PHONE TEST section below.
2. Still on the list from before: name the backup folder/file —
   needs the real phone (iCloud); ties into finishing the
   "Remember When" rename. (The when-you're-ready talk-throughs —
   Vault import, removing Vault categories, the backup Merge
   option — are in the DECISIONS section below.)


#40 & #41 STILL OPEN (what's left of your #41 list)
----------------------------------------------------------------------------
1. Name the backup folder — moved up from "someday" to "wanted." Best
   finished and checked on the real phone, where iCloud Drive is involved.
   (Now related: the app-name change back to "Remember When" touches the
   same backup file naming.)
3. Import files/documents into the Vault. Needs a talk-through first
   (what kinds of files, from where, how they're shown and protected).


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
(The old #56-#61 checklist rode your "beat on" build — you tested and
brought back five issues, all handled in #62. Anything from that old
list you DIDN'T flag is assumed fine; say so if something still nags.)
- THE ORDERS PAGE (#63), the whole thing on a real order or two.
  Mid-test results so far (reported live during #63):
  · PASSED: a reminder fired with HERE / OK / Delay 15-30-60 on it;
    Delay 15 dismissed the banner; the delayed banner CAME BACK at
    15 min; HERE from that banner logged the arrival correctly.
    That's the full snooze-and-arrive loop working on the phone.
  · PASSED: tapping the banner body opened the Orders page.
  · PASSED: the window-close reminder fired when it should have.
  · The day-before reminder (your Midday time) and morning-of
    reminder (your Morning time) firing on the right days — needs
    a real multi-day order to prove.
  · The flip side of window-close: NO nag on a package you'd
    already tapped HERE on before the window ended.
  · How the banner buttons list on the watch. One caution:
    banner-button registration once worked in the Sim but dropped
    a button set on the real phone (#39) — worth a careful look.
- PHONE-VERIFIED (#63, second build of the day): the wider tap
  areas on the Edit buttons + Settings gear — "easier to tap" —
  and the four-band bridge on all 14 pages. Both confirmed on
  the phone. Nothing left to check on either.
- The #62 fixes: ALL FIVE PASSED your phone test (reported #63) —
  Done-first banners, the four-band Home strip, the matched header
  heights, the bigger Home buttons, and the bigger spin buttons.
  Nothing left to check there.
(The old "To-Do fired early" test is CLOSED (#63): the test as
written needed the "At time" preset, which was removed back in #38 —
your call, reaffirmed #63 ("leave as is for now"). The early fire
was already explained as the 2-hour preset doing its job.)


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
(The single-digit date/time bug is FIXED — #58's control pads and
checks everything typed; an unpadded value can't be saved anymore.)
- Look Ahead: delaying from the BANNER doesn't show the "▶ Delayed"
  line on the tile (your #61-build phone test). Reading the code
  found the likely cause: the page only re-reads its saved items
  when it first opens, so a banner delay done while the page was
  already loaded never appears — and can even be lost. The Delay
  button ON the tile is fine. Fix is small (re-read on every visit).
  Also checked: a Delay is NOT supposed to write a log entry —
  only Done logs. That part is by design.
- My Week: marking a chore done from inside the app records today's date
  instead of the chore's own day. Minor.
- Tapping a reminder lands on the right page (tested #55), but taking
  you to the exact item on that page is still not built. Minor.


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Backup "Merge" option. Restoring currently REPLACES everything; you
  wanted the choice to MERGE instead. Its own session — each kind of data
  combines by its own rule, and those are your calls.
- Vault import (your item 3 above) — scope it out before building.
- Remove categories from Vault (your #47 idea) — talk-through first,
  like #42 did for To-Do.


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- Watch List overhaul — its own session, talk-through first (moved
  from What's Next, your call #55). The night-and-time-a-TV-show-airs
  idea folds into it.
- Rename the app back to "Remember When" — what's LEFT of it (#48,
  yours). The icon-label part was finished in #54.
  Still here: the backup file naming (Elyfont-Backup-...) and the
  file's internal marker — old backups must stay restorable. The
  "Memory Assist" tagline still needs a home.
- Project Planner has reminder fields on screen, but they aren't
  connected to anything yet.
- My Day / Pets: a Done tapped from the BANNER logs only the fire
  ("set") time. You want the entry to carry both times like the
  in-app logs do — the set time AND the time Done was actually
  tapped (#55).
  (The To-Do "no reminder set" save note: DONE in #58.)


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Moved here from What's Next (your call, #55): the structured
  reminder test checklist; My Week's done-date recording; exact-item
  tap routing; the dark startup flash; Planner's unconnected
  reminder fields. (Retiring Colors.ts: DONE in #57.)
- The Vault "Custom" label suspicion (spotted #47, NOT yet confirmed;
  parked #55, your call): typing your own label under "Custom" may
  save the word "Custom" instead. Test steps are in parked-items.md;
  if real, the fix is tiny.
- Voice control through Siri. Set aside after much effort; revisit only
  if a future iPhone update fixes the name-matching. (The app-name revert
  above assumes this stays shelved.)
- The Timer "Loud alert" isn't actually louder — a genuinely louder alarm
  needs a custom sound built into the app and special Apple permission to
  break through Silent / Do-Not-Disturb. Heavy — deferred.
- A running Timer's tile disappears if the app reloads or restarts (the
  list of active timers is only held in memory). Accepted as-is: "Done"
  still stops the alerts either way.
- Reminder times stay global — already decided, nothing to do.
