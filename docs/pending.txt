PENDING WORK — plain-language list
Last updated: 2026-07-04 (session #56 "Polish 1" — the Home page got
its polish and the To-Do banner got an OK button. Next build shows it
all on the phone.)

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- The app ("Remember When") is stable and on your phone via TestFlight.
  Nothing is half-built or broken in a way that's holding you up.
- Your phone (the #55 build) shows everything from #49 through #54:
  the new titles, Home 28/21, the two themes on all pages, and the
  "Remember When" icon label. The #48 Settings controls and nearly
  all the phone checkpoints were verified in #55.
- Session #56 "Polish 1" — built and approved by you in the
  Simulator ("the perfect balance"):
  · Home button names are now size 18 in Georgia in BOTH themes —
    gold in Dark, the usual blue in Light. (This fixes the
    too-small Dark buttons you spotted in #55.)
  · A soft halo now glows behind each Home button, in each theme's
    header color — this also fills the "too empty" button area.
  · The Light theme's ring around each button is now barely-there
    teal (#43a297), your pick from the mockups.
  · The home tile now reads "My Pets Day" (tile only, your call).
  · The To-Do banner has an OK button again: press and hold the
    banner, tap OK, it just closes — the app doesn't open. (The
    buttonless banner PASSED its test, but you decided you want
    the OK back after living with it.)
- NOT yet committed: the four #56 code files + the docs. You commit.
- #56's changes reach your phone at the NEXT batched build — judge
  the halo strength, ring shade, label wrapping, and the OK banner
  there. Build policy unchanged: batched builds only, you handle
  Expo billing.
- The dark startup flash is still there (a known small quirk).
- Small item still possible: the old Colors.ts file can be retired —
  nothing reads it anymore (on the someday list).


WHAT'S NEXT (#56) — suggested order, easiest and most useful first
------------------------------------------------------------------
1. The backup-keys bug — a good small Simulator session.
2. To-Do log rebuild — talked through in #55, spec ready (see the
   decisions section below).
3. The single-digit date/time padding fix (see STILL BROKEN below).
4. Name the backup folder/file — needs the real phone (iCloud); ties
   into finishing the "Remember When" rename.
   (The when-you're-ready talk-throughs — Vault import, removing
   Vault categories, the backup Merge option — are listed in the
   DECISIONS section below.)
(Done in #56: the Home dark-theme button fix, plus the halo, ring,
"My Pets Day", and the To-Do banner OK. Everything parked in #55 —
the Vault "Custom" check and the bigger/someday items — lives in
PARKED ON PURPOSE at the bottom.)


#40 & #41 STILL OPEN (from your #41 list, items 2, 5, 6, 7 done in
#42; item 4 phone-verified in #55)
----------------------------------------------------------------------------
- ✅ DONE — the buttonless To-Do banner test (#40): PASSED on the
  phone (#56) — and then overtaken the same session: you decided
  you want an OK button after all, and it's now built. The NEW
  one-OK banner gets its look at the next build (steps below).
1. Name the backup folder — moved up from "someday" to "wanted." Best
   finished and checked on the real phone, where iCloud Drive is involved.
   (Now related: the app-name change back to "Remember When" touches the
   same backup file naming.)
3. Import files/documents into the Vault. Needs a talk-through first
   (what kinds of files, from where, how they're shown and protected).
4. ✅ DONE — My Day banner "Done" on YESTERDAY'S reminder: PHONE-
   VERIFIED in #55 (tested just past midnight). Today's items stayed
   untouched; the past completion was logged under yesterday's date
   and fire time, as designed. (Spin-off request below in
   Nice-to-have: log BOTH times.)


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
- A To-Do reminder that seemed to fire early. Checking the code found no
  bug — almost certainly the "2 hours before" option doing its job. To be
  sure: make a task due a few minutes out, pick ONLY a short preset, and
  watch if it fires on the minute.
- The NEW To-Do banner OK (#56) — VERIFIED in the Simulator (press
  and hold a fresh banner with the app open → one OK button). Still
  give it a quick phone look at the next batched build: press and
  hold a To-Do banner, tap OK, the banner should close WITHOUT
  opening the app. (Banners scheduled on the old build stay
  buttonless until they cycle out.)
  LESSON from testing this (#56): the Simulator's LOCK SCREEN never
  shows banner buttons — not even My Day's proven six. Judge banner
  buttons with the app open in the Simulator, or on the real phone.
- Also at the next build: the Home page halo strength, the Light
  ring shade, and how the size-18 Georgia button names wrap — all
  Simulator-approved in #56, but the phone is the real judge.
  (Verified in #55: the shared routine popup (#39) — all 6 buttons on
  My Day / My Week / Pets, and tapping routes to the page; Look
  Ahead's reminders (#37); the Watch List once-over (#41); and the
  #42 once-over. The buttonless banner passed in #56 before the OK
  change replaced it.)


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
- To-Do dates/times typed with single digits (7/4/26 or 9:00 instead
  of 07/04/26 or 09:00) most likely break reminder scheduling
  SILENTLY — the code glues the typed text straight into a computer
  date without padding it (#55). Small fix: pad single digits
  automatically in the scheduling and sorting code. Until then:
  always type two digits, time in 24-hour clock.
- Backups skip some data (found #41, grew #48). The backup code has a
  fixed list of what to save; Look Ahead, Watch List, AND now the theme +
  popup choices were never added. Until fixed, restoring a backup loses
  those. Simulator-testable — a good small session.
- My Week: marking a chore done from inside the app records today's date
  instead of the chore's own day. Minor.
- Tapping a reminder now TRIED with a real tap (#55): it lands on the
  right page. Taking you to the exact item on that page is still
  not built. Minor.
  (The too-small Dark theme Home buttons from #55: FIXED in #56.)


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Backup "Merge" option. Restoring currently REPLACES everything; you
  wanted the choice to MERGE instead. Its own session — each kind of data
  combines by its own rule, and those are your calls.
- Vault import (your item 3 above) — scope it out before building.
- Remove categories from Vault (your #47 idea) — talk-through first,
  like #42 did for To-Do.
- To-Do log rebuild — TALKED THROUGH in #55, ready to build (a modest
  session of its own). The spec: drop the "📋 Log" button and its
  popup; the log sits at the bottom of the To-Do page, always visible
  under the task tiles, on the same pattern as the other pages' logs —
  header row with a "Clear All" button (with the same are-you-sure
  popup), tap an entry to edit its note, swipe left to delete it,
  last 50 kept, same styling. Each line keeps showing what it shows
  today: set date, done date, task name, notes.


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- Watch List overhaul — its own session, talk-through first (moved
  from What's Next, your call #55). The night-and-time-a-TV-show-airs
  idea folds into it.
- Rename the app back to "Remember When" — what's LEFT of it (#48,
  yours). The icon-label part was finished in #54.
  Still here: the backup file naming (Elyfont-Backup-...) and the
  file's internal marker — old backups must stay restorable. The
  "Memory Assist" tagline still needs a home.
- Project Planner has reminder fields on screen, but they aren't
  connected to anything yet.
- My Day / Pets: a Done tapped from the BANNER logs only the fire
  ("set") time. Patrick wants the entry to carry both times like the
  in-app logs do — the set time AND the time Done was actually
  tapped (#55).
- To-Do: saving a task with a due date but NO reminder preset picked
  schedules nothing, silently (#55 — this cost an hour of test
  waiting). Add a small note on save, like "no reminder set for
  this task."


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Moved here from What's Next (your call, #55): the structured
  reminder test checklist; My Week's done-date recording; exact-item
  tap routing; retiring Colors.ts; the dark startup flash; Planner's
  unconnected reminder fields.
- The Vault "Custom" label suspicion (spotted #47, NOT yet confirmed;
  parked #55, your call): typing your own label under "Custom" may
  save the word "Custom" instead. Test steps are in parked-items.md;
  if real, the fix is tiny.
- Voice control through Siri. Set aside after much effort; revisit only
  if a future iPhone update fixes the name-matching. (The app-name revert
  above assumes this stays shelved.)
- The Timer "Loud alert" isn't actually louder — a genuinely louder alarm
  needs a custom sound built into the app and special Apple permission to
  break through Silent / Do-Not-Disturb. Heavy — deferred.
- A running Timer's tile disappears if the app reloads or restarts (the
  list of active timers is only held in memory). Accepted as-is: "Done"
  still stops the alerts either way.
- Reminder times stay global — already decided, nothing to do.
