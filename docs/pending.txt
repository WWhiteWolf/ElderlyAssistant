PENDING WORK — plain-language list
Last updated: 2026-07-04 (session #60 "Polish 5" — To-Do's dates are
stored as numbers now, Priority and Status are gone from To-Do, the
"30 min." reminder button is in, and the New/Edit popup got a good
polish.)

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- The app ("Remember When") is stable and on your phone via TestFlight
  (the #55 build). Nothing is half-built or broken in a way that's
  holding you up.
- Session #60 "Polish 5" — built and approved by you in the Simulator:
  · To-Do now stores its dates and times as plain numbers, the same
    way Look Ahead does — the last page that kept them as text.
    Old tasks carry no weight (your rule): they open at today-noon
    when edited and convert themselves the first time you save.
    You watched a reminder fire exactly on the minute.
  · Priority (Urgent/Normal/Someday) and Status (Active/On Hold/
    Done) are GONE from To-Do — the form is much shorter, and the
    tile's green Done button is the one way to complete a task.
  · A "30 min." one-tap reminder button now sits before "1 hour".
  · The New/Edit popup got polish: taller (nearly full screen),
    tighter under the title, the reminder buttons moved UP under
    the date/time so they can't be overlooked anymore, and Notes
    sits last with a "↓" hint and a roomier three-line box.
- NOT yet committed: the #60 code (app/todo.tsx) + docs. You commit.
- Two of the four pre-build steps are done. What's left before your
  "beat on" EAS build: just the four time-only pages (item 1 below).
- The dark startup flash is still there (a known small quirk).


WHAT'S NEXT — the order you set in #59
------------------------------------------------------------------
1. The time-only pages get the date/time setter: My Day, My Week,
   Pets, Settings — probably two a session (next session or two).
2. Your EAS phone build + load — the "beat on" build — and judge
   everything waiting in NEEDS A PHONE TEST below.
3. After the build: name the backup folder/file — needs the real
   phone (iCloud); ties into finishing the "Remember When" rename.
   (The when-you're-ready talk-throughs — Vault import, removing
   Vault categories, the backup Merge option — are in the DECISIONS
   section below.)
(Done in #60: To-Do dates as numbers + the form slim-down + the
30-min button + popup polish. Done in #59: Look Ahead on the shared
setter + the stuck-red-box fix.)


#40 & #41 STILL OPEN (what's left of your #41 list)
----------------------------------------------------------------------------
1. Name the backup folder — moved up from "someday" to "wanted." Best
   finished and checked on the real phone, where iCloud Drive is involved.
   (Now related: the app-name change back to "Remember When" touches the
   same backup file naming.)
3. Import files/documents into the Vault. Needs a talk-through first
   (what kinds of files, from where, how they're shown and protected).


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
- A To-Do reminder that seemed to fire early. Checking the code found no
  bug — almost certainly the "2 hours before" option doing its job. To be
  sure: make a task due a few minutes out, pick ONLY a short preset, and
  watch if it fires on the minute.
- The To-Do banner OK (#56) — verified in the Simulator twice now
  (#56 and again in #57). Still give it a quick phone look: press
  and hold a To-Do banner, tap OK, the banner should close WITHOUT
  opening the app. (Banners scheduled on the old build stay
  buttonless until they cycle out.)
  LESSON (#56): the Simulator's LOCK SCREEN never shows banner
  buttons — judge banner buttons with the app open, or on the phone.
- The Home page polish (#56): halo strength, the Light ring shade,
  and how the size-18 Georgia button names wrap.
- The new To-Do log page (#57): a quick once-over on the phone.
- The new date/time control on To-Do (#58): spin, type, watch the
  padding, try a bad value, and the "no reminder" confirm — plus
  how the smaller spinners feel on the real screen.
- The same control on Look Ahead (#59): New/Edit an entry, spin and
  type, try a bad value, clear it (red should vanish), and check
  the reminders still arm from the saved date.
- The #60 To-Do changes: dates stored as numbers (make a task, watch
  its reminder fire on the minute; edit an OLD task — it should open
  at today-noon and be fine after saving), the slimmed form (no
  Priority, no Status), the "30 min." button, and the reworked popup
  (reminder buttons visible under the time, Notes last with the ↓).


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
(The single-digit date/time bug is FIXED — #58's control pads and
checks everything typed; an unpadded value can't be saved anymore.)
- My Week: marking a chore done from inside the app records today's date
  instead of the chore's own day. Minor.
- Tapping a reminder lands on the right page (tested #55), but taking
  you to the exact item on that page is still not built. Minor.


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Backup "Merge" option. Restoring currently REPLACES everything; you
  wanted the choice to MERGE instead. Its own session — each kind of data
  combines by its own rule, and those are your calls.
- Vault import (your item 3 above) — scope it out before building.
- Remove categories from Vault (your #47 idea) — talk-through first,
  like #42 did for To-Do.


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- Watch List overhaul — its own session, talk-through first (moved
  from What's Next, your call #55). The night-and-time-a-TV-show-airs
  idea folds into it.
- Rename the app back to "Remember When" — what's LEFT of it (#48,
  yours). The icon-label part was finished in #54.
  Still here: the backup file naming (Elyfont-Backup-...) and the
  file's internal marker — old backups must stay restorable. The
  "Memory Assist" tagline still needs a home.
- Project Planner has reminder fields on screen, but they aren't
  connected to anything yet.
- My Day / Pets: a Done tapped from the BANNER logs only the fire
  ("set") time. You want the entry to carry both times like the
  in-app logs do — the set time AND the time Done was actually
  tapped (#55).
  (The To-Do "no reminder set" save note: DONE in #58.)


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Moved here from What's Next (your call, #55): the structured
  reminder test checklist; My Week's done-date recording; exact-item
  tap routing; the dark startup flash; Planner's unconnected
  reminder fields. (Retiring Colors.ts: DONE in #57.)
- The Vault "Custom" label suspicion (spotted #47, NOT yet confirmed;
  parked #55, your call): typing your own label under "Custom" may
  save the word "Custom" instead. Test steps are in parked-items.md;
  if real, the fix is tiny.
- Voice control through Siri. Set aside after much effort; revisit only
  if a future iPhone update fixes the name-matching. (The app-name revert
  above assumes this stays shelved.)
- The Timer "Loud alert" isn't actually louder — a genuinely louder alarm
  needs a custom sound built into the app and special Apple permission to
  break through Silent / Do-Not-Disturb. Heavy — deferred.
- A running Timer's tile disappears if the app reloads or restarts (the
  list of active timers is only held in memory). Accepted as-is: "Done"
  still stops the alerts either way.
- Reminder times stay global — already decided, nothing to do.
