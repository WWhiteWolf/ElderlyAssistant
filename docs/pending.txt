PENDING WORK — plain-language list
Last updated: 2026-07-02 (session #47 — Shopping and Vault now have
both themes, designed with mockups and checked by you in the Simulator.
Five pages done, eight to go. Full rollout order is in parked-items.md
under "TWO-THEME ROLLOUT.")

This is the "what's still left" list in everyday words. It's pulled from
parked-items.md and the hand-off notes so you can glance at it without
reading the technical docs. Nothing here is urgent. Finished work isn't
tracked here — git history keeps the full record — so this list stays short.


WHERE THINGS STAND RIGHT NOW (read this first)
----------------------------------------------
- The app ("Remember When") is stable and on your phone via TestFlight.
  Nothing is half-built or broken in a way that's holding you up.
- Session #47: the Shopping page and the Vault now follow both themes.
  You approved every piece from mockups first, then checked both themes
  in the Simulator. Your calls this session: in dark, quiet buttons
  (Stocked, Edit, Cancel) are gold OUTLINES while solid orange means
  "act on this"; and all the dark theme's cream text got brighter,
  which also brightened Backup and Watch List to match. Light theme
  is unchanged everywhere.
- Small wording fix you asked for: Vault's "Missing Info" popup now
  says "Tap a Label above, then enter a Value." so it's clear the
  labels want a tap.
- Two new parked items: a SUSPECTED Vault bug (typing your own label
  under "Custom" may save the word "Custom" instead — not yet
  confirmed; test steps are in parked-items.md), and your request to
  eventually REMOVE CATEGORIES FROM VAULT (needs a talk-through first).
- One thing to know: iPhone Settings light/dark does NOT change your
  app's colors — only the one word in constants/Themes.ts does. But
  Apple's own popups inside the app (alerts, share sheet, keyboard)
  DO follow the iPhone setting — you saw exactly this when the white
  "Missing Info" popup appeared over the dark Vault. Decision parked
  for the Settings-toggle session.
- Until the Settings toggle is built, the active theme is one word in
  constants/Themes.ts (DEFAULT_THEME) — currently set to 'light' from
  your testing. Set it to the one you want before committing.
- Not yet committed — you commit this session's code + docs when ready.
- Next session (#48): timer.tsx + settings.tsx — the Switch toggles
  need per-theme colors, and it's the natural time to decide about the
  in-app theme toggle and those Apple popups.


YOUR #41 LIST — WHAT'S STILL OPEN (items 2, 5, 6, 7 done in #42)
----------------------------------------------------------------------------
1. Name the backup folder — moved up from "someday" to "wanted." Best
   finished and checked on the real phone, where iCloud Drive is involved.
3. Import files/documents into the Vault. Needs a talk-through first
   (what kinds of files, from where, how they're shown and protected).
4. My Day: a banner "Done" on YESTERDAY'S reminder checked off TODAY'S.
   Most likely your phone is still running the older build from before the
   fix (#39) — the fix just hasn't been phone-tested yet. You're tracking
   whether it happens again; it's on the structured-test checklist.


NEEDS A PHONE TEST (built, just not confirmed on the phone yet)
----------------------------------------------------------------------------
- A To-Do reminder that seemed to fire early. You recall setting one for
  23:30 and it went off around 21:15. Checking the code found no bug — that's
  exactly 2 hours before a 23:15 time, so it was most likely the "2 hours"
  before-reminder option doing its job. To be sure: make a new task due a few
  minutes out, pick ONLY a short preset, and watch if it fires on the minute.
- The buttonless To-Do banner (#40), the shared routine popup (#39), Look
  Ahead's reminders (#37), a Watch List once-over (#41), and now a #42
  once-over (To-Do without categories, Look Ahead popup buttons, the
  My Day / Pets un-check) — all batched into the phone checkpoints.


STILL BROKEN OR UNFINISHED (small bugs)
---------------------------------------
- Backups skip Look Ahead and Watch List (found #41). The backup code has a
  fixed list of what to save, and those pages' data was never added. Until
  fixed, restoring a backup loses the Look Ahead and Watch List lists.
  Simulator-testable — a good small session.
- My Week: if you mark a chore done from inside the app (not from the
  notification), it records today's date instead of the chore's own day.
  Minor — only matters if you log a chore well after its day.
- Tapping a reminder takes you to the right screen but not to the exact
  item, and this has never actually been tried with a real tap.
  (The To-Do Custom Category cancel bug is GONE — #42 removed categories.)


DECISIONS TO MAKE WHEN YOU'RE READY
-----------------------------------
- Backup "Merge" option. Right now restoring a backup REPLACES everything on
  the phone. You wanted the choice to MERGE a backup into what's already
  there instead. That needs its own session because each kind of data
  (tasks, logs, counters, settings) has to combine by its own rule, and
  those are your calls.
- Vault import (your item 3 above) — scope it out before building.


NICE-TO-HAVE LATER (polish, low priority)
-----------------------------------------
- A Theme toggle button in Settings, so you can switch between light and
  dark. The groundwork now exists (#45's shared theme file) — the toggle
  itself is a small session once more pages are converted. No rush.
- Project Planner has reminder fields on screen, but they aren't connected
  to anything yet.
- Finish the "Elyfont" renaming: the in-app home greeting still says
  "Remember When"; the TestFlight / App Store listing is still named
  "Remember When"; and the "Memory Assist" tagline still needs a home
  (it can't go in the app's name).
- Make the button labels match: To-Do says "New Task" while Vault says
  "+ Add."
- Add a Snooze button on each To-Do item itself. My Day and Pets Day already
  have one on every tile; To-Do only has Snooze on the notification banner.


PARKED ON PURPOSE (bigger items, on hold for now)
-------------------------------------------------
- Voice control through Siri. It gets you to the app and tapping works, but
  Siri won't recognize spoken item names ("mark snack done"). After a lot of
  effort this was set aside. Revisit if a future iPhone update fixes it.
- The Timer "Loud alert" isn't actually louder — it's the normal alert tone.
  A genuinely louder, distinct alarm needs a custom sound file built into the
  app (a full rebuild), and breaking through Silent / Do-Not-Disturb needs a
  special Apple permission meant for medical/safety apps. Heavy — deferred.
- A running Timer's tile disappears if the app reloads or restarts (the list
  of active timers is only held in memory). Accepted as-is: tapping "Done"
  still stops the alerts whether or not the tile is showing.
- Reminder times stay global — already decided, nothing to do. The Morning /
  Midday / Evening times are set once in Settings and apply to everything.
  You considered letting a single appointment use its own times and chose
  not to. This note only exists in case you ever want to revisit that.
